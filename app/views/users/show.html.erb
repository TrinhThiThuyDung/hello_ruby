<p id="notice"><%= notice %></p>

<p>
  <b>Name:</b>
  <%= @user.name %>
</p>

<p>
  <b>Email:</b>
  <%= @user.email %>
</p>

<p>
  <b>Content Microposts:</b>
  <%= id=@user.id
  microposts = Micropost.where(user_id: @user.id).pluck(:content)
  if microposts!=nil
  	microposts.each do |micropost|
  	  micropost
  	end
  end
  
  %>

</p>
<%= link_to 'Edit', edit_user_path(@user) %> |
<%= link_to 'Back', users_path %>
